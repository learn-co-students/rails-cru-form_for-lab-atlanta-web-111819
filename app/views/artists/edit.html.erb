<%= form_for @artist do |f| %>
<label> Edit Artist</label>
  <%= f.label :name %>
  <%= f.text_field :name %>
   <%= f.label :bio %>
  <%= f.text_area :bio %>
  <%= f.submit "Update Artist" %>
<% end %>