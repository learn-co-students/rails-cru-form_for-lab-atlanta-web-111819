<%= form_for @artist do |f| %>
<label>Create Artist</label>
  <%= f.label :name %>
  <%= f.text_field :name %>
   <%= f.label :bio %>
  <%= f.text_area :bio %>
  <%= f.submit "Create Artist" %>
<% end %>